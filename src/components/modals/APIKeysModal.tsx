import React from 'react';

/**
 * APIKeysModal has been eliminated from the client-facing UI.
 * All API keys are managed securely via server-side environment variables on Vercel.
 */
export const APIKeysModal: React.FC = () => {
  return null;
};

export default APIKeysModal;
